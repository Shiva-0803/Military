databases:
  - name: military-db
    databaseName: military
    user: military_user
    plan: free

services:
  # BACKEND
  - type: web
    name: military-backend
    runtime: python
    region: singapore
    plan: free
    rootDir: server_django
    buildCommand: ./build.sh
    startCommand: gunicorn config.wsgi:application
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: military-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.0

  # FRONTEND (Corrected for Render Blueprint)
  - type: web
    name: military-frontend
    runtime: static # This specifies it's a static site
    region: singapore
    plan: free
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist # Distinguishes static site from dynamic web service
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: military-backend
          property: url
